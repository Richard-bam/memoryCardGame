import React from "react";

// basic header component to be displayed on every page
function Header() {
    return (
        <div className="headerDiv">
            <h1>Richard's Memory Card Game</h1>
        </div>
    );
}

export default Header;
